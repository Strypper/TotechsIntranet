<Page
    x:Class="IntranetUWP.Views.FoodOrderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IntranetUWP.Views"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls" 
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:usercontrols="using:IntranetUWP.UserControls" xmlns:data="using:IntranetUWP.Models"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d" Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="320"/>
        </Grid.RowDefinitions>
        <controls:AdaptiveGridView x:Name="FoodGridView" animations:ReorderGridAnimation.Duration="300" HorizontalContentAlignment="Center"
                                   DesiredWidth="355" StretchContentForSingleRow="False" OneRowModeEnabled="False" Padding="0" SelectionMode="Single">
            <controls:AdaptiveGridView.ItemContainerTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition IsStaggeringEnabled="True" FromHorizontalOffset="400" FromVerticalOffset="450"/>
                </TransitionCollection>
            </controls:AdaptiveGridView.ItemContainerTransitions>
            <controls:AdaptiveGridView.ItemTemplate>
                <DataTemplate x:DataType="data:FoodDTO">
                    <usercontrols:FoodCard FoodName="{x:Bind FoodName}" FoodEnglishName="{x:Bind FoodEnglishName}"/>
                </DataTemplate>
            </controls:AdaptiveGridView.ItemTemplate>
        </controls:AdaptiveGridView>
        <Pivot Grid.Column="1" Grid.RowSpan="2">
            <PivotItem Padding="0">
                <PivotItem.Header>
                    <Image Source="/Assets/iDealogic.png" Width="100" Height="50"/>
                </PivotItem.Header>
                <ListView ItemsSource="{x:Bind Users}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <usercontrols:MemberCard/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>
            <PivotItem>
                <PivotItem.Header>
                    <Image Source="/Assets/Devinition.png" Width="100" Height="50"/>
                </PivotItem.Header>
                <ListView ItemsSource="{x:Bind Users}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <usercontrols:MemberCard/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>
        </Pivot>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="500"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Bapcaitim" Foreground="Red" FontWeight="Bold" FontSize="30" Margin="5"/>
                <TextBlock Text="Deadline for choosing your dish is 12pm Thursday. If you haven't chosen a dish by the deadline, it means that you won't have any dish prepared for you."
                           Foreground="Black" Margin="5,0,0,0" TextWrapping="Wrap"/>
                <TextBlock Foreground="Black" Margin="5,-30,0,0">
                    <Run Text="2" FontSize="150"/>
                    <Run Text="days" FontSize="30"/>
                    <Run Text="until menu close"/>
                </TextBlock>
            </StackPanel>
            <Image Source="ms-appx:///Assets/LunchFood.svg" Grid.Column="1" Margin="10" VerticalAlignment="Top"/>
            <StackPanel Grid.Column="2">
                <TextBox Header="Food Name" RequestedTheme="Light" CornerRadius="2"/>
                <TextBox Header="Food VietNamese Name" RequestedTheme="Light" CornerRadius="2"/>
                <TextBlock Text="MIÊU TẢ" Foreground="Black" FontWeight="Bold" HorizontalAlignment="Center"
                           Margin="5"/>
                <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
                    <RadioButton Content="🍚 Rice" RequestedTheme="Light"/>
                    <RadioButton Content="🍝 Spagheti" RequestedTheme="Light"/>
                    <RadioButton Content="🥖 Bread" RequestedTheme="Light"/>
                    <RadioButton Content="🥗 Vegetarian" RequestedTheme="Light"/>
                </StackPanel>
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
                        <RadioButton Content="🥩 Meat" RequestedTheme="Light"/>
                        <RadioButton Content="🍗 Chicken" RequestedTheme="Light"/>
                        <RadioButton Content="🍤 Shrim" RequestedTheme="Light"/>
                        <RadioButton Content="🍳 Fried Egg" RequestedTheme="Light"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
                    <RadioButton Content="🍱 Default" RequestedTheme="Light"/>
                    <RadioButton Content="🍜 Noodle" RequestedTheme="Light"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <CommandBar Background="{ThemeResource SystemControlAcrylicElementBrush}" IsOpen="False" DefaultLabelPosition="Right"
                    VerticalAlignment="Bottom" RequestedTheme="Light" Grid.Row="1">
            <AppBarButton Icon="Add" Label="Add Food"/>
            <AppBarButton Icon="Edit" Label="Edit" IsEnabled="False"/>
            <AppBarButton Icon="Delete" Label="Delete food" IsEnabled="False"/>
            <AppBarButton Icon="Delete" Label="Delete entire list" Foreground="Red"/>
            <AppBarButton Label="Notified the team">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xEA8F;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
    </Grid>
</Page>
